# 로컬 실행과 공개 데이터 경계

## 운영 전제

SignalDesk는 1인 로컬 리서치 MVP입니다. 기본 서버는 `127.0.0.1`에 바인딩합니다. 같은 출처 검사와 요청 토큰은 외부 웹페이지의 무단 유료 호출을 줄이기 위한 조치이지, 사용자 인증·권한 관리·상용 보안 검증을 대체하지 않습니다. 기존 호환 API도 남아 있으므로 그대로 인터넷에 공개하지 않습니다.

## 비밀정보

API 키는 `.env.signaldesk`에 평문으로 저장되며 키 값은 화면·명령행 인자로 전달하지 않습니다. 설정 스크립트의 숨김 입력을 사용하세요. `.cache/`에는 SEC 식별용 이메일, 분석 초안, 저장 결과 등 공개 대상이 아닌 정보가 포함될 수 있습니다.

`.env*`, `.cache/`, `.backups/`, `logs/`, `.git` 과거 이력, DB, 개인 분석 JSON, 원본 설치 ZIP은 공개본에 넣지 않습니다. 스크린샷에도 이메일·키·전체 상태 API 응답을 포함하지 않습니다. 원문 링크의 개인 토큰·추적 파라미터도 확인합니다.

## 외부 요청

가격 요청, 동의한 AI 요청, 동의한 SEC 요청을 구분합니다. SEC 이메일은 해당 요청 식별용 헤더에 포함됩니다. 조회 실패 또는 접근 제한을 우회하지 않습니다. 유료 요청에는 별도 동의와 시도 한도가 있으며 자동 유료 재시도는 하지 않습니다. 실패해도 이미 전송된 요청에 비용이 발생할 수 있습니다.

검색 자료는 모델에 주는 데이터로 취급하고 명령으로 신뢰하지 않도록 프롬프트와 입력 경계를 구성했습니다. 그러나 프롬프트 인젝션, 잘못된 인용, 의미 왜곡을 완전히 방어한다고 주장하지 않습니다.

## GitHub 공개 전

파일 제외 규칙과 패턴 검사는 보조 장치입니다. staged 파일 목록과 diff를 직접 검토하세요. `.gitignore`는 이미 추적된 비밀을 지우거나 과거 커밋을 정리하지 않습니다. 노출된 키가 있으면 제공처에서 우선 폐기·교체하고 해당 이력을 별도로 처리해야 합니다. 새 공개용 폴더를 만든다고 이미 공개된 비밀이 회수되는 것은 아닙니다.

공개 이슈에 실제 키나 개인 원문을 붙이지 마세요. 재현은 비밀값을 제거한 최소 입력과 오류 코드로 작성합니다.

참고: [GitHub의 파일 제외 안내](https://docs.github.com/en/get-started/git-basics/ignoring-files), [민감정보 제거 안내](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository).
